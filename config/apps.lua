-- =========================================================
-- APPLICATION COMMANDS
-- =========================================================
-- Centralized application commands.
-- Change applications here without touching keybinds.
-- =========================================================

local apps = {
    terminal = "kitty",
    fileManager = "kitty --class yazi -e yazi",
    menu = "rofi -show drun",
}

return apps
