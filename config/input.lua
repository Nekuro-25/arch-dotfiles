-- =========================================================
-- INPUT
-- =========================================================

hl.config({
    input = {
        -- Keyboard
        kb_layout = "us",
        kb_variant = "",
        kb_model = "",
        kb_options = "",
        kb_rules = "",

        -- Pointer focus
        follow_mouse = 1,

        -- Default sensitivity
        sensitivity = 0,

        -- Touchpad
        touchpad = {
            natural_scroll = true,
        },
    },
})
